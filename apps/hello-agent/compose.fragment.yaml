services:
  hello-agent:
    # Use pre-built GHCR image by default
    image: ghcr.io/nmdimas/a2a-hello-agent:main
    # Fallback to local build if needed for development
    build:
      context: ./apps/hello-agent
    container_name: hello-agent
    labels:
      - "com.a2a.agent=true"
      - "com.a2a.agent.id=hello-agent"
      - "com.a2a.agent.network=a2a-network"
    networks:
      - a2a-network
    env_file:
      - ./apps/hello-agent/.env.local
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped
