{% extends 'admin/layout.html.twig' %}

{% block title %}{{ 'channels.conversations.title'|trans }} — Admin{% endblock %}
{% block page_title %}{{ 'channels.conversations.title'|trans }}{% endblock %}

{% block content %}
    {# Stats row #}
    <div class="stats-grid" style="margin-bottom:1.5rem;">
        <div class="stat-card">
            <div class="stat-label">{{ 'channels.conversations.stats.total'|trans }}</div>
            <div class="stat-value">{{ stats.total }}</div>
        </div>
        <div class="stat-card">
            <div class="stat-label">{{ 'channels.conversations.stats.active'|trans }}</div>
            <div class="stat-value text-success">{{ stats.active }}</div>
        </div>
        <div class="stat-card">
            <div class="stat-label">{{ 'channels.conversations.stats.with_threads'|trans }}</div>
            <div class="stat-value">{{ stats.with_threads }}</div>
        </div>
        <div class="stat-card">
            <div class="stat-label">{{ 'channels.conversations.stats.recently_active'|trans }}</div>
            <div class="stat-value text-success">{{ stats.recently_active }}</div>
        </div>
    </div>

    <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:1rem;">
        <p style="color:var(--text-muted);font-size:.9rem;margin:0;">{{ 'channels.conversations.subtitle'|trans }}</p>
        <a href="{{ path('admin_channel_instances') }}" class="btn btn-settings">
            {{ 'channels.conversations.manage_instances'|trans }}
        </a>
    </div>

    {% if conversations is empty %}
        <div class="glass-card">
            <div class="stub-content" style="min-height:200px;">
                <svg viewBox="0 0 24 24" width="48" height="48" fill="currentColor" style="opacity:.3;"><path d="M20 2H4c-1.1 0-2 .9-2 2v18l4-4h14c1.1 0 2-.9 2-2V4c0-1.1-.9-2-2-2zm0 14H5.17L4 17.17V4h16v12zM7 9h2v2H7zm4 0h2v2h-2zm4 0h2v2h-2z"/></svg>
                <p>{{ 'channels.conversations.empty'|trans }}</p>
            </div>
        </div>
    {% else %}
        <div class="glass-card" style="padding:0;overflow:hidden;">
            <div style="overflow-x:auto;">
                <table class="admin-table">
                    <thead>
                        <tr>
                            <th>{{ 'channels.conversations.table.title'|trans }}</th>
                            <th>{{ 'channels.conversations.table.type'|trans }}</th>
                            <th>{{ 'channels.conversations.table.instance'|trans }}</th>
                            <th>{{ 'channels.conversations.table.members'|trans }}</th>
                            <th>{{ 'channels.conversations.table.features'|trans }}</th>
                            <th>{{ 'channels.conversations.table.status'|trans }}</th>
                            <th>{{ 'channels.conversations.table.last_activity'|trans }}</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for conv in conversations %}
                            {% set is_left = conv.left_at is not null %}
                            <tr style="{{ is_left ? 'opacity:.5;' : '' }}">
                                <td>
                                    <div style="font-weight:500;">{{ conv.title ?? ('channels.conversations.unknown_title'|trans) }}</div>
                                    <div style="font-size:.75rem;color:var(--text-muted);font-family:monospace;">{{ conv.chat_id }}</div>
                                </td>
                                <td>
                                    {% if conv.type == 'supergroup' %}
                                        <span class="badge badge-channel">{{ 'channels.conversations.type.supergroup'|trans }}</span>
                                    {% elseif conv.type == 'group' %}
                                        <span class="badge badge-channel">{{ 'channels.conversations.type.group'|trans }}</span>
                                    {% elseif conv.type == 'channel' %}
                                        <span class="badge" style="background:rgba(168,85,247,.2);color:#a855f7;">{{ 'channels.conversations.type.channel'|trans }}</span>
                                    {% elseif conv.type == 'private' %}
                                        <span class="badge">{{ 'channels.conversations.type.private'|trans }}</span>
                                    {% else %}
                                        <span class="badge">{{ conv.type }}</span>
                                    {% endif %}
                                </td>
                                <td>
                                    {% if conv.bot_username is defined and conv.bot_username %}
                                        <span style="font-family:monospace;font-size:.85rem;">@{{ conv.bot_username }}</span>
                                    {% elseif instance_map[conv.bot_id] is defined %}
                                        <span style="font-family:monospace;font-size:.85rem;">@{{ instance_map[conv.bot_id].bot_username }}</span>
                                    {% else %}
                                        <span style="color:var(--text-muted);">{{ conv.bot_id[:8] }}…</span>
                                    {% endif %}
                                </td>
                                <td>
                                    {% if conv.member_count %}
                                        <span>{{ conv.member_count|number_format }}</span>
                                    {% else %}
                                        <span style="color:var(--text-muted);">—</span>
                                    {% endif %}
                                </td>
                                <td>
                                    <div style="display:flex;gap:.3rem;flex-wrap:wrap;">
                                        {% if conv.has_threads %}
                                            <span class="badge" style="background:rgba(99,102,241,.2);color:#818cf8;" title="{{ 'channels.conversations.feature.threads_hint'|trans }}">
                                                {{ 'channels.conversations.feature.threads'|trans }}
                                            </span>
                                        {% endif %}
                                    </div>
                                </td>
                                <td>
                                    {% if is_left %}
                                        <span class="badge badge-disabled">{{ 'channels.conversations.status.left'|trans }}</span>
                                    {% elseif conv.joined_at %}
                                        <span class="badge badge-enabled">{{ 'channels.conversations.status.active'|trans }}</span>
                                    {% else %}
                                        <span class="badge badge-unknown">{{ 'channels.conversations.status.unknown'|trans }}</span>
                                    {% endif %}
                                </td>
                                <td>
                                    {% if conv.last_message_at %}
                                        {% set last = conv.last_message_at %}
                                        {% if last is instanceof('\\DateTimeImmutable') %}
                                            <span class="trace-time" title="{{ last|date('Y-m-d H:i:s') }}">
                                                {{ last|date('d.m.Y H:i') }}
                                            </span>
                                        {% else %}
                                            <span class="trace-time">{{ conv.last_message_at }}</span>
                                        {% endif %}
                                    {% else %}
                                        <span style="color:var(--text-muted);">—</span>
                                    {% endif %}
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    {% endif %}
{% endblock %}
